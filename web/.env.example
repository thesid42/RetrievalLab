# Leave blank when the API is served from the same origin.
VITE_API_BASE_URL=

# Optional Vite development proxy. If set, use an empty VITE_API_BASE_URL.
VITE_API_PROXY=http://127.0.0.1:8000

# Set to true to always use the built-in hackathon demo data.
VITE_DEMO_MODE=false
